services:
  trading-agents:
    build: .
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - .:/app
      - ./logs:/app/logs
      - ${OBSIDIAN_VAULT_PATH:-./obsidian_vault}:/app/obsidian_vault
